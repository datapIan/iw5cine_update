/*
 *      IW5Cine
 *      .menu callbacks
 *
 *      Will be worked on once the rest is finished wink wink
 */

await()
{
    AssertMsg( "[IW5cine] ui::await() not ready yet" );
    return;

    /*
    for(;;)
    {
        self waittill("menuresponse", menu, response);
        something
    }
    */
}